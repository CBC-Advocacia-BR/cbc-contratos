{
  "name": "cbc-contratos",
  "version": "1.0.0",
  "description": "CBC Contratos — plataforma interna do escritorio (contrato -> assinatura -> cobranca -> processo).",
  "scripts": {
    "dev": "cd client && npm run dev",
    "dev:client": "cd client && npm run dev",
    "test": "cd client && npm test",
    "test:coverage": "cd client && npm run test:coverage",
    "build": "cd client && npm run build",
    "lint": "cd client && npm run lint",
    "deploy": "cd client && ./deploy.sh",
    "rollback": "cd client && ./rollback.sh",
    "verificar": "npm run test && npm run build && npm run lint:gate && npm run check:functions && npm run check:edge",
    "check:edge": "cd client && npm run check:edge",
    "check:functions": "cd client && npm run check:functions",
    "lint:gate": "cd client && npm run lint:gate"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "engines": {
    "node": ">=22"
  }
}
